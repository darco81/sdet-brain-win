name: series-status
description: Synthesise current state of a Series (publication plan, blockers, timeline)
tool: multi_query_search
args:
  limit: 8
  per_query_limit: 6
query_template: "Series {{number}} publication status, blockers, timeline"
default_args:
  number: "01"
output_format: markdown
